def print_numbers():
    numbers = [1, 2, 3, 4, 5]
    
    for i in range(len(numbers)):  # Iterate over the length of the list
        print(f"Number: {numbers[i]}")  # Access each element using the index

print_numbers()
#attempt3